import $$HomeStack from 'generated/router/components/HomeStack';

export default class HomeStack extends $$HomeStack {
  constructor(basePath: string, name: string, routeIndex: number) {
    super(basePath);
  }
}
